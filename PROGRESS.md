## Status
- **Current week:** Week 2 — COMPLETE ✅ (quiz: 4/6, gaps noted: explicit context passing, stale-session handling)
- **Next:** Week 3 — Tool Design & MCP (new chat: "Week 3 - MCP")

## Log
- 2026-07-22 · Week 0 done: repo, venv (Python 3.12.13), anthropic SDK, API key as env var, first API call.
- 2026-07-22 · Week 1 done: tool_use round trip, agentic loop, parallel tool calls, 5-tool order assistant, programmatic refund prerequisite, escalation handoff. Quiz done.
- 2026-07-23 · Week 2 done: Agent SDK (query/options), subagents via AgentDefinition + Task tool, context isolation proven, parallel spawn, PreToolUse refund-guard hook, sessions (resume/fork), Exercise 4 research pipeline with preserved source attribution. Quiz: pending.

## Week 3 — COMPLETE (Tool Design & MCP, Domain 2)
Files: bad_descriptions(_v2).py, good_descriptions.py, error_responses.py, forced_choice.py, order_server.py (FastMCP: 4 tools + catalog resource + $500 server-side refund gate), mcp_client_test.py, refund_e2e_test.py, edit_trap.py, .mcp.json (project scope, ${ENV} expansion), notes/week3-tool-design-mcp.md
Exercise 1 (exam guide): COMPLETE — differentiated descriptions, structured errors, business-rule gate, structured escalation handoff, multi-concern handling.
Quiz: 10/12. Drill list for week 7: any-vs-forced boundary, Grep-vs-Glob content/name distinction.
Next: Week 4 — Claude Code Configuration & Workflows (Domain 3, 20%). Open a new chat titled "Week 4 - Claude Code".

## Week 4 — IN PROGRESS (Claude Code Configuration, Domain 3)
- 2026-07-30 · Lesson 4.1 done: CLAUDE.md hierarchy (user/project/directory), @import via notes/style-guide.md, /memory diagnostics. Found a pre-existing power-user template in ~/.claude/CLAUDE.md; backed up (CLAUDE.md.backup) and trimmed — a live TS 3.1 case. Verified: directory CLAUDE.md loads on file touch (not at startup); demo_ prefix + docstring + English rules applied from three different layers in one file. Quiz: 6/6.
- Next: Lesson 4.2 — path-scoped rules (.claude/rules/ with glob frontmatter).
- 2026-07-30 · Lesson 4.2 done: path-scoped rules (.claude/rules/ + paths glob frontmatter). A/B test proved pattern-based loading (py triggered, md did not; same folder). Debugging story: rule file added mid-session was not picked up — stale session inventory, fixed by fresh session (Week 2 principle recurs). New diagnostics: /memory for startup inventory, "Loaded <file>" lines for in-session triggers. Observed rule-vs-personal-guideline conflict surfaced by Claude instead of silently resolved. Also fixed instruction-reality drift (.venv reference). Quiz: 6/6 (second in a row).
- Next: Lesson 4.3 — custom slash commands and skills (context: fork, allowed-tools, argument-hint).
- 2026-07-30 · Lesson 4.3 done: custom slash commands (.claude/commands/, $ARGUMENTS, argument-hint) and skills (SKILL.md frontmatter: context: fork, allowed-tools). Built /exam-drill command and read-only repo-audit skill; fork isolation observed live (21-file scan returned as a short summary). Audit caught pre-rule files missing EXAM TAKEAWAY; retrofit done in main session (auditor finds, main session fixes — allowed-tools as a lock). Audit → fix → re-audit loop completed. Quiz: 5/6; drill item: paths = loading condition (rules) vs context: fork = output isolation (skills).
- Next: Lesson 4.4 — plan mode vs direct execution.
- 2026-07-31 · Lesson 4.4 done: plan mode vs direct execution. Task A (single-line edit, direct: read-change-done) vs Task B (Week 7 harness design in plan mode): two parallel Explore agents, critical-finding behavior (missing 6-scenario list flagged as blocker instead of fabricated — interview pattern live), approval gate + Esc → zero file changes (git status clean). Added notes/ccarf-scenarios.md to close the gap for Week 7. Decision rule sealed: stated complexity is an input, not a surprise — start in plan mode. Quiz: 6/6.
- Next: Lesson 4.5 — iterative refinement (input/output examples, test-driven iteration, interview pattern, batch-vs-sequential fixes).
- 2026-07-31 · Lesson 4.5 done: iterative refinement. Experiment A (vague prose): silent assumptions (ValueError, E.164) disclosed only as a trailing note — guidance, not guarantee, again. Experiment B (test-first): 4 given cases + 10 edge cases, first run 8 failed/6 passed, single root cause (ValueError vs None contract), 2-line fix → 14 passed — validation-retry loop live in pytest. Experiment C (interview pattern): surfaced unanticipated design space (validation strictness, 7-digit local, special numbers, output shape). Chef rule sealed: interacting fixes together, independent fixes sequentially. Quiz: 6/6.
- Next: Lesson 4.6 — Claude Code in CI/CD (-p, --output-format json, --json-schema, independent review instance) — closes Week 4.
- 2026-07-31 · Lesson 4.6 done: headless CI reviewer (-p, --output-format json, --json-schema as inline JSON). Independent instance caught docstring-vs-code validation gap + missing EXAM TAKEAWAY lines; findings fixed via find→fix→verify loop. Quiz: 6/6.

## Week 4 — COMPLETE ✅ (Claude Code Configuration, Domain 3)
Quiz total: 35/36. Drill list for week 7: paths (rules) vs context: fork (skills).
Files: CLAUDE.md (root+week4), notes/style-guide.md, .claude/rules/python-demos.md, .claude/commands/exam-drill.md, .claude/skills/repo-audit/SKILL.md, normalize_phone.py + tests, review_schema.json, notes/ccarf-scenarios.md, notes/week4-claude-code.md
Next: Week 5 — Prompt Engineering & Structured Output (Domain 4, 20%). Open a new chat titled "Week 5 - Structured Output".
- 2026-07-31 · Lesson 5.1 done: prompt-only JSON vs forced tool_use extraction. Arm A: 3/3 json.loads failures (markdown fence), total_amount as string, currency as raw 'TL', run 3 appended prose outside the fence - same prompt, different shape. Arm B: 3/3 byte-identical, float type, enum normalized to 'TRY', comma-decimal handled via schema description. stop_reason end_turn vs tool_use. Quiz: 4/5; miss = schema fixes syntax/types, NOT semantics (line items not summing to total -> validation layer, lesson 5.4).
- Next: Lesson 5.2 - schema design against hallucination (nullable fields, enum + "other" + detail).
- 2026-07-31 · Lesson 5.2 done: schema design against hallucination. Nullable (["string","null"]) + explicit "never estimate" description vs closed required field; enum + "other" + *_detail for extensible categories. Live: v1 substituted the issue date for a missing due_date (plausible, ISO-valid, schema-compliant, wrong) and collapsed an unlisted payment method into the nearest enum value - both failures silent. required = key present, value may be null. Union types are grammar-expensive: nullable only where the source may genuinely lack data.
- Next: Lesson 5.3 - few-shot prompting for format consistency and ambiguous cases.
- 2026-07-31 · Lesson 5.3 done: few-shot prompting. Key finding: prose matched few-shot on documents with strong explicit signals - few-shot is the fix for an OBSERVED inconsistency, not a default first move (same ordering as tool descriptions before few-shot in Week 3). Real gap appeared on the payment-notice document: prose picked 'other' while its own reasoning stated collection + zero balance -> enum values read as title labels rather than semantic categories; few-shot generalized the payment-obligation rule from a differently-titled example. Corollary: 'other' needs an explicit usage criterion in its description or it becomes a cheap escape hatch. Few-shot also tightened free-text field wording (format consistency observed live). Schema = syntax/type, nullable = absence, few-shot = judgment on ambiguity.
- Next: Lesson 5.4 - validation-retry loops (Pydantic, semantic vs syntax errors, when retry cannot help).
  - Quiz 5.3: 4.5/6. Miss 1: "be conservative" / "only high-confidence" prompts do NOT reduce false positives - explicit categorical criteria do (lesson 5.5 territory); few-shot is not a precondition for a system prompt to work. Miss 2: multiple-response item - selected 1 of 2 required answers (B+C). DRILL FOR WEEK 7: (a) any answer relying on the model filtering its own confidence is almost always wrong; (b) re-read the required answer count on multiple-response items.
- 2026-07-31 · Lesson 5.4 done: validation-retry loops. Pydantic field_validator vs model_validator(mode="after") = the semantic layer JSON Schema cannot express; error text returned as tool_result (role user, Week 1 mechanic put to work). CAUGHT ON TAPE: retry made things worse where the error class was wrong for it. doc2 (source self-contradictory): attempt 2 changed a line item 300 -> 350 to satisfy the validator - falsified record marked VALID. doc3 (info absent): attempt 1 "<UNKNOWN>" (honest, rejected on type), attempt 2 issue_date copy (rejected on ordering), attempt 3 fabricated +30 days - a surrender curve; every rejection taught it that honesty was wrong. Legitimate retry was ONLY the a1->a2 format repair. Arm B single pass, zero fabrication: stated vs calculated + conflict_detected + nullable due_date -> clean / human review / absent. Instrumentation lesson: log the SUCCESS payload too, or "green" hides the fabrication. Mature systems branch by error class (Week 3 errorCategory/isRetryable pattern applied to validation): format -> retry, semantic conflict -> human, absence -> policy default.
- Next: Lesson 5.5 - explicit criteria for false positive reduction + multi-instance/multi-pass review.
  - Quiz 5.4: 5/6. Miss: picked "line items don't sum to stated total" as retry-fixable - the exact doc2 case observed live minutes earlier (retry falsified a line item 300->350). Concept is understood (Q1 answered correctly), the reflex is not yet automatic. DRILL FOR WEEK 7: three-column test - information present but malformed -> retry; information absent -> nullable + policy; information present but self-contradictory -> flag + human. Retry belongs to column one only.
- 2026-07-31 · Lesson 5.5 done: explicit criteria vs vague confidence language (TS 4.1) + multi-pass/independent review (TS 4.6). Round 1 with unambiguous defects: both arms 3/3, 0 FP - experiment did not separate them (same lesson as 5.3: prompt work wins under AMBIGUITY, not against obvious signals). Round 1 tells: vague arm graded IndexError as critical next to SQL injection (no severity definitions -> severity label loses meaning), and invented ad-hoc detected_pattern wording (ungroupable, so dismissal analysis impossible). Round 2 with borderline patterns, 3 runs each: vague = 3/2/3 findings, load_config flagged in 2/3 runs at two different severities (a CI tool that contradicts itself destroys trust in its accurate findings); categorical = 3/3/3 stable. KEY FINDING: vague missed parse_amount's silent 0.0 fallback 0/3 while categorical caught it critical 3/3 - criteria are a search plan, not just a filter; recall rose with precision. detected_pattern field enables counting which construct drives dismissals so high-FP categories can be disabled while their prompts are fixed. TS 4.6 self-review vs independent instance was already proven live in lesson 4.6 (independent reviewer caught the docstring-code gap the producing session left).
- Next: Lesson 5.6 - Message Batches API (closes Week 5).
  - Quiz 5.5: 4/6. Miss 1 (Q5): chose "run 3 passes, report findings appearing in >=2" over per-file passes + separate integration pass. Consensus filtering suppresses real bugs caught intermittently while letting consistently-produced noise through - proven by this lesson's own data (vague arm: load_config 2/3 would PASS a 2-of-3 rule; parse_amount 0/3 could never be rescued by it). Root cause was attention dilution; repeating the same pass repeats the dilution. RULE: "run N times, take the majority" is almost always wrong in Domain 4.
  - Miss 2 (Q6): multiple-response item answered with the same option twice; correct = B (consistency) + D (criteria direct where the model looks). SECOND multiple-response format error in two quizzes (5.3 Q6 was the first) - this is now a pattern, not an accident. HABIT: read the last line of the item first; count the required answers on fingers before writing.
  - Week 5 FINAL QUIZ: 10/12 (same as Week 3). Full credit on the hardest distinctions, including two items previously missed and now closed: retry boundary (5.4 miss) and the consensus-filtering trap (5.5 miss).
  - Miss 1 (Q6, SLA arithmetic): answered "every 24h" for a 28h SLA. Forgot the QUEUE leg — worst case is wait + processing = 24 + 24 = 48h. Correct = 4h. Remember the formula has two legs, not one.
  - Miss 2 (Q3, half): picked "remove the enum entirely" alongside the correct *_detail field. Removing the enum destroys categorization (the "TL" case from 5.2). Correct pair = closed enum + "other" AND a nullable *_detail.
  - Miss 3 (Q8, half): picked model-generated confidence_score with a threshold — the exact drill item recorded twice in this file. A model whose calibration is the problem cannot be its own filter. Correct pair = explicit report/skip categories AND temporarily disabling high-FP categories while their prompts are fixed. RED FLAGS: "self-reported confidence", "only high-confidence findings", "be conservative".
  - FORMAT: 4th multiple-response error across the week's quizzes (5.3 Q6, 5.5 Q6, final Q3, final Q8) — content knowledge is there, the loss is in reading/checking. WEEK 7 PROCEDURE: mark every option true/false independently, then count trues against the required number, then narrow.
